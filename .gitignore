# Local build output produced by the CI workflow; never committed.
dist/

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Built bundle — generated from src/ by node build.mjs. NOT committed: CI builds it into the release .zip.
/extension.js
